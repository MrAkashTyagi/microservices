
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MrAkashTyagi/microservice-tut-config
          clone-on-start: true

server:
  port: 8085
